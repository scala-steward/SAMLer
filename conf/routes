# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET         /                                   controllers.HomeController.index


## SAML flow
# Initiate flow with authn
GET         /oauth2/default/v1/authorize        controllers.HomeController.index

# ACS
POST        /sso/saml2/:idpId                   controllers.HomeController.index2(idpId)
## End SAML flow

## IDP Management
GET         /idps/:idpId                        controllers.HomeController.index2(idpId)

POST        /idps                               controllers.HomeController.index

PUT         /idps/:idpId                        controllers.HomeController.index2(idpId)

DELETE      /idps/:idpId                        controllers.HomeController.index2(idpId)
## End IDP Management